# @PAD: gap-285-p3-closed-wall-deny
# @GCDE: gaplune.policy.v1
[package]
name = "gap-closed-wall-deny"
version = "0.1.0"
edition = "2021"
description = "GAP-285-P3. Classic GAP closed-wall Deny. enabled is load-time. Retry reseals."
license = "UNLICENSED"
publish = false

[[bin]]
name = "gap-closed-wall-deny"
path = "src/main.rs"
