# Changelog

Public GAP open source. README teaching and the public package tree are not the same set of work.
This file records dated product changes. It does not claim the public tree is fully upgraded.

## 2026-09-12 - Public teaching versus remaining package holes

Live README teaching already names collect-all Admit after freeze-at-seal and the 1000 ms kernel pulse.
Layer 1 is optional authoring. Constrained decoding is not Admit.
Live trust bundle teaching is sha256-structure. Presence of trust_ring is Deny.
Who-may teaching on the public site is agent_may. That website work is GAP-WEB-P1 through GAP-WEB-P5 on NLA-website.

Remaining public-tree tickets are still backlog:
GAP-OSS-PERM-P0 public who-may field must become the live kernel permission field.
GAP-OSS-RANK-P0 leftover rank wall id must match the live leftover rank field with presence Deny.
GAP-OSS-HASH-P0 closed-wall capsule digest must be sha256-structure. The package still hashes with FNV-1a.
GAP-OSS-LIC-P1 public GAP package license files must be Apache-2.0. Package license files still say UNLICENSED.
GAP-OSS-FIT-P1 fit document must name remaining public-tree holes instead of claiming none remain.
GAP-OSS-RT-P0 teaching envelope package runs collect-all then Apply on a teaching wall subset. Package coding waits plan approval.
GAP-OSS-RT-P1 teaching runtime loads, seals, freezes, waits and Applies after Admit. Package coding waits plan approval.
GAP-OSS-CDE-P0 scaffold public constrained decode package with byte grammar after plan approval.
GAP-OSS-CDE-P1 public constrained decode engine must take language model API inputs. Package coding waits plan approval.

Website teaching alignment is not a public-tree package close.
